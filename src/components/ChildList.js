import React from 'react'

function ChildList({name}) {
    return (
        <div>
            {/* <h2>Name is {person.name} and city is {person.city}</h2> */}
    <h2>name is {name}</h2>
        </div>
    )
}

export default ChildList
